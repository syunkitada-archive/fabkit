# Webapp

``` bash
# create database
./manage.py migrate
./manage.py syncdb

# start test server
./manage.py runserver
```

``` bash
# Install Node.js

# install npm packages
$ npm install -g grunt-cli
$ npm install

# start grunt
$ grunt
```
